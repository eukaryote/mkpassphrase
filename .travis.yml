sudo: false
language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
  - "3.7-dev"
  - "pypy"
  - "pypy3"

# command to install dependencies
install:
  - pip install pip==18.0 tox-travis .
# command to run tests
script: tox
branches:
  only:
    - master
