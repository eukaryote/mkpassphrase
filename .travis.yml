language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "pypy"
# command to install dependencies
install:
  - pip install .
# command to run tests
script: py.test
branches:
  only:
    - master
